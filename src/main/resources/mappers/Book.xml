<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--扫描的工作由application.properties中的是指自动完成-->
<mapper namespace="com.example.reader.mapper.BookMapper">
    <update id="updateScore">
        update book b set evaluation_score = (
            select nullif(avg(score), 0) from evaluation e where e.book_id = b.book_id and state = 'enable'
            ),evaluation_quantity = (
            select nullif(count(*), 0) from evaluation e where e.book_id = b.book_id and state = 'enable'
        )
    </update>
</mapper>

